---
title: "Questions for interviews"
source: https://www.notion.so/eb95e8867bba4d7f9040680541b0f1ca
notion-id: eb95e886-7bba-4d7f-9040-680541b0f1ca
parent: "The Best Notes of the F Word"
tags: [notion-import]
---
# Questions for interviews

## let vs var vs const:

The principal defence in var and let is the scope.

var has a global scop and let and const only have a block scop

const is a constant once is defined we can’t redefined or reeasinate  only can mutate.

## What is a Template string or **Template literals** in JS:

Are strings that allow add JS expression inside of this strings, These are enclosed by the back-tick (`)

## What is DOM:

 The **<u>Document Object Module </u>**is the representation of the object that compose the structure of document on the web.

## Call stack and task queue:

In js exist a complex mecanism that have the work to organiza the excuton time the we have the call stack and have the wort to excute all the

## What is event:

An event can be triggered by the user action e.g. clicking the mouse button or tapping keyboard, or generated by APIs to represent the progress of an asynchronous task.

**Microtask Queue** things: `.then()`, `.catch()`, `.finally()` , `await` , MutationObserver, queueMicrotask 

NOTE : this queue has priority upper the **task Queue**

: `setTimeout()`, `setInterval()` , `onClick`, `onKeyPress` , `click`, `scroll`, `keypress`, `mousedown` ,

## What is visual dom in react :

The virtual DOM (VDOM) is  “virtual”, representation of a UI is kept in memory and synced with the “real” DOM by a library ReactDOM

## What a call-back function:

A callback function is a function passed into another function as an argument, this function passed like argument is executed principal function.

## Who manage an event JS:

in the blower we can listen a the tings that happen in the most popular is a

```
buttonContainer.addEventListener('click', event => {
  console.log(event.target.value)
})
```

## What is the difference between == and === operators:

1. (===) cheek if the values are exactly equal in both levels, level type and value level

1. (==): is a little beat more relaxed that not check both levels only check the value and does not always check the types

## **What are lambda or arrow functions:**

An arrow function is a concise form of a function expression and doesn’t bind its own `this`, `arguments`, `super`, or `new.target`.

## **What is a pure function:**

is function that is no affected by external code the return code is only determined  by its own parameters.

##  What is Hook in React:

Is a special function in Recat that allows to use React features like modify lifecyle of a componte or save spacial values like states  etc.

## What is useEffect in React:

Is a hook that allows execute code when the component is assembled an when array dependencies are affected by a mutation or assignment 

## What is useMemo in React:

React useMemo() hook is a function that save in cache a value, this hook is use-full because we can save value that could affect the performance of the page to avoid value are recaulculat when the commontes is re-rendered.

## What is useRef in React:

The useRef Hook allows you to persist values between renders. it can save Dom Elements when this value or element of the dom is changed not execute a re-render of react.

## What is a promise in Js:

Is a representation of eventual susses or failure data is a asynchronous proses.

## What is a h**oisting:**

When js start to analyse the code move all the definition variables and definition of functions  of code to the top of the scop or context but when we try to access before of the real definition are undefined

## How works even loop:

Is the mechanism that js use for execute all the task or proses tack of a call Stack and Task queue

## How get of module of operation:

is with % example

`3 % 3 === 0  `

## What is a clases in JS:

In JavaScript, classes are a way to define and create objects, providing a more structured and object-oriented approach to programming.

## What is a destructuring in JavaScript.

is a feature in JavaScript that allows you to extract values from arrays or properties from objects

## What is **Prototype in JS:**

In JavaScript, a prototype is a mechanism that allows inheritance of properties and methods between objects.

## What is Redux:

Is a library commonly used with React to global states is recomendable only used in a big apps because add a lot concept an rules 

## What is **Store in Redux**:

JavaScript object with the responsibility that keep the state of application this obj is read-only. is only modified dispatching actions. 

## What is Reducer** in Redux**:

Actions are plain JavaScript objects that describe a change in the application's state.

Actions must have a `**type**` property indicating the type of action success , faild and loading

## What is **Dispatch in Redux**:

The `**dispatch**` function is used to send actions to the Redux store when an action is dispatched, the store calls the correct reducer(s) to update the state.

## What is **Middleware in Redux**:

It sits between the dispatching of an action and the moment it reaches the reducer.

we can use to trigger another action or reeintent API call o insects data to action 

## What is **Selectors in Redux**:

Selectors are functions used to extract specific pieces of data from the Redux store.

## genarator funcion : 

is an especial type of funtion that can pause his execution and resume in any moment keep his state
